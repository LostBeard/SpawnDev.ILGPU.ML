<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFramework>net10.0</TargetFramework>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
		<AllowUnsafeBlocks>true</AllowUnsafeBlocks>
	</PropertyGroup>

	<ItemGroup>
		<PackageReference Include="SpawnDev.UnitTesting.Browser" Version="2.7.0" />
		<PackageReference Include="SpawnDev.SpawnJS.Blazor" Version="2.1.17" />
	</ItemGroup>

	<ItemGroup>
		<ProjectReference Include="..\SpawnDev.ILGPU.ML\SpawnDev.ILGPU.ML.csproj" />
		<!-- Optional WebTorrent delivery. SpawnDev.ILGPU.ML no longer references WebTorrent; the test
		     project keeps this so the torrent IModelSource stays exercised by the suite. It also supplies
		     SpawnDev.AsyncFileSystem, which these tests used to get transitively through ML. -->
		<ProjectReference Include="..\SpawnDev.ILGPU.ML.WebTorrent\SpawnDev.ILGPU.ML.WebTorrent.csproj" />
	</ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\SpawnDev.Phonemizer\SpawnDev.Phonemizer.csproj" />
  </ItemGroup>
</Project>
