<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AssemblyName>ModelInspector.Console</AssemblyName>
    <RootNamespace>SpawnDev.ILGPU.ML.Examples.ModelInspectorConsole</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- The only dependency: the ML library, for ModelInspectorHelper. No GPU/accelerator needed —
         inspection is pure header parsing. -->
    <ProjectReference Include="..\..\SpawnDev.ILGPU.ML\SpawnDev.ILGPU.ML.csproj" />
  </ItemGroup>

</Project>
